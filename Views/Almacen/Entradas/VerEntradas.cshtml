@model List<Entrada>
@{
    ViewData["Title"] = "Silo Manager";
}

<div style="width: 100%; padding-bottom: 20px">
    <div>
        <div>
            <h2>Herramienta/Dispositivos regresados al almacén</h2>
        </div>
        <div>
            <a href="~/../Almacen/Entradas/AgregarEntradas/@ViewBag.ProyectoId">Agregar elemento</a>
        </div>
    </div>
</div>

<div class="table-responsive">
    <table class="table table-striped">
        <thead>
        <tr>
            <th scope="col">Modelo/Serie</th>
            <th scope="col">Artículo</th>
            <th scope="col">Cantidad</th>
            <th scope="col">Fecha</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var entrada in @Model)
        {
            <tr>
                <th scope="row">@entrada.ArtModelo</th>
                <td>@ViewBag.Context.CatArticulos.Find(@ViewBag.Context.InvArticulos.Find(entrada.ArtModelo).ArtId).ArtNombre</td>
                <td>@entrada.Cantidad</td>
                <td>@entrada.Fecha</td>
            </tr>
        }
        </tbody>
    </table>
</div>
